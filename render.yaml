services:
  - type: web
    name: depo-api
    runtime: node
    buildCommand: corepack enable && yarn install --immutable
    startCommand: yarn workspace backend node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
      - key: CORS_ORIGINS
        value: "*"
